import { useServerSideEffect } from 'webshift';
import axios from 'axios';

export default () => {

    return (
        <div>
            { console.log("<Welcome/>") }
            <h2>Welcome</h2>

        </div>
    );
}